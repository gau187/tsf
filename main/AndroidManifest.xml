<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tsf.mycredibleinfo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity
            android:name=".MainActivity"
            android:label="SignUp" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".PersonalDetailsActivity"
            android:label="Personal Details" />
        <activity
            android:name=".UpdateProfileActivity"
            android:label="Update Profile" />
        <activity
            android:name=".EducationDetailsActivity"
            android:label="Education Details" />
        <activity
            android:name=".UpdateEducationActivity"
            android:label="Update Education" />
        <activity android:name=".ProfessionalDetails"
            android:label="Professional Details"/>
        <activity android:name=".UpdateProfessionalActivity"
            android:label="Update Professional"/>
    </application>

</manifest>